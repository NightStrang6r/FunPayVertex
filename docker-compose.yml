version: '3.3'

services:
  funpay:
    build: ./
    container_name: funpay-vertex 
    restart: unless-stopped
    tty: true
    volumes:
      - ./configs:/app/configs
      - ./storage:/app/storage
      - ./logs:/app/logs
